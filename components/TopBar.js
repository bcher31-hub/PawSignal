export default function TopBar() {
  return (
    <div style={styles.bar}>
      PawSignal
    </div>
  );
}

const styles = {
  bar: {
    textAlign: "center",
    fontSize: 20,
    fontWeight: 700,
    paddingBottom: 4,
  },
};
